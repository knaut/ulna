var root = function() {
	
}
// treat root as the top-level app component, or the universal store
// on the client side it could be associated with the window
// on the server side, it could serve up requests

// maybe it serves the role of the dispatcher, a shared message registry
root.component = {

}